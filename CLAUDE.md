# CLAUDE.md

## このリポジトリについて

`tech-self-study` は、物語仕立てで手を動かしながら学ぶ自習用教材集(**fable-101 シリーズ**)。
各教材(`0N-xxx-fable-101/`)は `chapters/01〜16` に 1 章 1 ファイルの Markdown が並び、
コード例・演習・背景コラム(💡ポイント / 📜歴史の背景 / ⚙️内部の仕組み、教材によっては
🐍 Python比較 / 🐹 Go比較 / 🔍 なぜそうなっているの?)で構成されている。詳細は各教材の
`README.md` と、リポジトリ直下の `README.md`(全体マップ)を参照。

## ユーザーと一緒にチャプターを進める学習セッションの進め方(重要)

ユーザーが「チャプターNから一緒に進めたい」のように学習セッションを求めてきたときは、
以下のスタイルを守ること(これは特定の教材に限らず、今後すべての言語教材で共通のやり方)。

1. **コードは実行前に必ず提示し、ユーザーの明示的な許可(「実行して」等)を待ってから実行する。**
   提示しただけで待たずに実行しない。ユーザーが「実行せずに次へ」と言ったら、結果を見せずに
   次のトピックの説明へ進む。
2. ユーザーが概念について質問したとき(「なぜ〜?」「〜のイメージが掴めない」「メリットは?」等)は、
   **その場で丁寧に答えてから** 次のトピックに進む。既存の教材本文をただ読み上げるのではなく、
   対話として深掘りに付き合う。図・具体例・他言語(Python/Go/TypeScript など既習言語)との比較を
   積極的に使うと理解が進みやすい。
3. セッション中の深掘り説明(内部の仕組み・比較・「なぜそうなっているのか」)を、ユーザーから
   「ドキュメントに反映して」と頼まれたら、該当する章の Markdown ファイルに、その教材で
   既に使われているコラム記法に合わせて追記する(例: `> 🔍 なぜそうなっているの?` の並びに
   `> ⚙️ 内部の仕組み` を新設するなど)。教材ごとの文体・絵文字の使い方・物語の設定
   (工房、劇場、食堂など)を壊さないこと。
4. 追記したら作業ブランチへコミットし、そのまま作業ブランチへプッシュしてよい。
   **`main` への直接反映(マージ・プッシュ)は、ユーザーから明示的に指示があったときだけ行う。**
   指示があれば、作業ブランチを `main` に fast-forward マージしてプッシュする(PR を経由しない
   直接反映で構わない)。指示がなければ作業ブランチに留め、必要ならPR作成の要否を確認する。
5. 章の最後(演習)まで来たら、演習に軽く触れたうえで次章への案内をする。演習を無理に解かせず、
   ユーザー自身でやるか代わりに実行するかを確認する。
6. セッションの区切りでは、その日にカバーした範囲を短くまとめ、次に進む章へのリンクを示す。

## Ruby コード実行時の注意

- `ruby -e '...'` に日本語(マルチバイト文字)を含めると `invalid multibyte char (US-ASCII)` で
  落ちることがある。`LANG=C.utf8 ruby -e '...'` を使うか、スクラッチパッド配下に `.rb` ファイルを
  書き出してから `ruby path/to/file.rb` で実行する。
- 動作確認用の一時ファイルはリポジトリ内に置かず、セッションのスクラッチパッドディレクトリを使う。
